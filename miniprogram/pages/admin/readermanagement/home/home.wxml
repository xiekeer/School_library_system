<!--pages/admin/readermanagement/home/home.wxml-->
<view class="cu-list grid col-{{gridCol}} gridBorder margin-top">
    <view class="cu-item" wx:for="{{iconList}}" wx:key wx:if="{{index<gridCol*3}}">
      <view class="cuIcon-{{item.icon}} text-{{item.color}}">

      </view>
      <text>{{item.name}}</text>
    </view>
  </view>
